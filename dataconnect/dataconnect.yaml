specVersion: "v1beta"
serviceId: "token_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "token_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
